{
  "name": "love-line-backend",
  "version": "1.0.0",
  "description": "Twilio-based phone matching service for Valentine's Day blind dates",
  "main": "src/index.ts",
  "scripts": {
    "build": "pnpm rebuild better-sqlite3 && tsc",
    "start": "node dist/index.js",
    "dev": "sh -c \"ngrok http 3000 --log=stdout >/tmp/ngrok-dev.log 2>&1 & ts-node-dev --respawn --transpile-only src/index.ts\"",
    "type-check": "tsc --noEmit",
    "validate": "ts-node src/utils/validate-participants.ts",
    "db:seed": "ts-node src/db/seed.ts",
    "db:seed:prod": "node dist/db/seed.js",
    "db:reset": "rm -f data/participants.db data/participants.db-wal data/participants.db-shm && npm run db:seed",
    "postinstall": "pnpm rebuild better-sqlite3"
  },
  "author": "",
  "license": "MIT",
  "engines": {
    "node": "24.9.0",
    "pnpm": "10.28.1"
  },
  "packageManager": "pnpm@10.28.1",
  "dependencies": {
    "better-sqlite3": "^12.6.2",
    "dotenv": "^17.2.4",
    "express": "^5.2.1",
    "kysely": "^0.28.11",
    "twilio": "^5.12.1"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/express": "^4.17.21",
    "@types/node": "^20.10.0",
    "ts-node": "^10.9.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.3.3"
  }
}
